import { TrustStrip } from 'website';

// Self-contained band: the sector list is baked into the component, so there
// is exactly one true rendering of it.
export const Default = () => <TrustStrip />;
