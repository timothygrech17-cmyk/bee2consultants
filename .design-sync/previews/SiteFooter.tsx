import { SiteFooter } from 'website';

// Self-contained: nav columns, contact details and legal line all live in the
// component, so there is one true rendering.
export const Default = () => <SiteFooter />;
